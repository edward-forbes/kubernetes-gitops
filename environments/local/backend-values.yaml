replicaCount: 1

image:
  repository: hashicorp/http-echo
  tag: "1.0"

message: "Hello from Docker Desktop Kubernetes"

service:
  type: ClusterIP
  port: 8080

replicaCount: 1

image:
  repository: hashicorp/http-echo
  tag: "1.0"

service:
  type: ClusterIP
  port: 8080

message: "Hello from the backend"

ingress:
  enabled: false
  className: nginx
  host: localhost
  path: /api